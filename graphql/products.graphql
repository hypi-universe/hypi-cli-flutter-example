query ProductsData($arcql: String!)
{
    find(type:Product, arcql: $arcql){
        edges {
            node{
                ... product
            }
        }
    }
}

fragment product on Product {
    hypi {
        id
    }
    title
    description
}